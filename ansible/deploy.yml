---
- name: Deploy
  hosts: devopspetclinic.westeurope.cloudapp.azure.com
  become: true
  tasks:
    - name: Run deploy
      ansible.builtin.import_role:
        name: deploy
        tasks_from: deploy
